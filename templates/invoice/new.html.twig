{% extends 'base.html.twig' %}

{% block title %}Nouvelle Facture{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="card shadow-sm">
        <div class="card-header bg-info text-white">
            <h3 class="mb-0">💰 Créer une nouvelle facture</h3>
        </div>
        <div class="card-body">
            {{ form_start(form, {'attr': {'class': 'row g-3'}}) }}

                <div class="col-md-6">
                    {{ form_label(form.client, null, {'label_attr': {'class': 'form-label'}}) }}
                    {{ form_widget(form.client, {'attr': {'class': 'form-control'}}) }}
                    {{ form_errors(form.client) }}
                </div>

                <div class="col-md-6">
                    {{ form_label(form.clientEmail, null, {'label_attr': {'class': 'form-label'}}) }}
                    {{ form_widget(form.clientEmail, {'attr': {'class': 'form-control'}}) }}
                    {{ form_errors(form.clientEmail) }}
                </div>

                <div class="col-md-6">
                    {{ form_label(form.amount, null, {'label_attr': {'class': 'form-label'}}) }}
                    {{ form_widget(form.amount, {'attr': {'class': 'form-control'}}) }}
                    {{ form_errors(form.amount) }}
                </div>

                <div class="col-md-6">
                    {{ form_label(form.status, null, {'label_attr': {'class': 'form-label'}}) }}
                    {{ form_widget(form.status, {'attr': {'class': 'form-select'}}) }}
                    {{ form_errors(form.status) }}
                </div>

                <div class="col-md-6">
                    {{ form_label(form.assignedTo, null, {'label_attr': {'class': 'form-label'}}) }}
                    {{ form_widget(form.assignedTo, {'attr': {'class': 'form-select'}}) }}
                    {{ form_errors(form.assignedTo) }}
                </div>

                <div class="col-12 mt-3">
                    <button type="submit" class="btn btn-success btn-lg">
                        💾 Enregistrer la facture
                    </button>
                    <a href="{{ path('app_invoice_index') }}" class="btn btn-secondary btn-lg ms-2">
                        ⬅ Retour
                    </a>
                </div>

            {{ form_end(form) }}
        </div>
    </div>
</div>
{% endblock %}
